// Same page as the bare /vs/[a]/[b] route; the network comes from the URL,
// which the battle card reads through useNetwork like every other page.
export { default } from '@/components/VersusPage';
